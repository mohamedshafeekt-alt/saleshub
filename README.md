# Sales CRM Platform — Backend

FastAPI backend for InnoBoon's Sales Prospecting & CRM Platform, Phase 1.
See `CLAUDE.md` for full context, scope, and working conventions.

## Stack
- Python (uv for dependency management)
- FastAPI
- SQLAlchemy + Alembic
- PostgreSQL
- pytest / pytest-asyncio / httpx

## Setup
```bash
uv sync
uv run alembic upgrade head
uv run uvicorn app.main:app --reload
```

## Running checks
```bash
uv run ruff check .
uv run mypy app
uv run pytest
```

## Implemented
_(updated after every feature — do not batch these)_

- [x] Login / JWT auth (`POST /api/v1/auth/login`, admin-only `POST /api/v1/users`, `scripts/seed_admin.py`)
- [x] RBAC (Sales Rep, Delivery SME, Sales Manager/Director, Admin) — `require_role(...)` dependency
- [x] Admin-create-user no longer accepts a client-supplied password: server generates one and emails it via `app.services.email` (`EmailSender` protocol, `SMTPEmailSender`, injected through `get_email_sender`); `UserCreate`/`User` gained `first_name`/`last_name`
- [x] `created_at`/`updated_at` moved onto the shared `Base` — every future model gets them for free
- [x] Global error handling (`app/core/error_handler.py`): unhandled DB errors and unexpected exceptions return a structured `ErrorResponse`; deliberate `HTTPException`s (401/403/409 etc.) untouched. Simplified `app/core/logging.py` replaces ad-hoc logging.
- [x] Config now loads from a single `APP_CONFIG` JSON blob in `.env` instead of one env var per setting
- [x] Enums moved to `app/models/enums.py` (starting with `UserRole`), re-exported from `app.models.user` for compatibility
- [x] Lead CRUD + filter/search (owner, source, status, company/owner-name search) — single `POST /api/v1/leads` upsert route (no `id` in the body creates, `id` present partially updates — replaces the old separate create/PATCH routes), plus `GET/DELETE /api/v1/leads`; Sales Rep/Delivery SME/Manager/Admin all have full access; Sales Rep and Delivery SME are scoped to leads they own plus unassigned leads (`owner_id IS NULL`, a shared claimable queue) — Manager/Admin see everything
- [x] Lead duplicate-email detection (409, same pattern as User)
- [x] Lead `tier` removed entirely (kickoff-email scope call — Account keeps `tier`, only Lead's was dropped); `owner_id` is optional at creation (a lead can be unassigned); Lead has a `status` field (`not_contacted` default, `attempted_to_contact`/`contacted`/`contact_in_future`/`junk_lead`/`lost_lead`), settable on create/update and filterable via `GET /api/v1/leads?status=`
- [x] `id` moved onto the shared `Base` alongside `created_at`/`updated_at` — every model gets it for free, no more per-table `id` boilerplate
- [x] Admin gets an email the moment any lead is created (`send_new_lead_notification_email`, same best-effort/non-blocking pattern as the new-user-credentials email — a failed send never fails the request)
- [x] `lead_contacts` table — a Lead can have more than one contact; creating a Lead auto-inserts its own email/phone as the primary `lead_contacts` row, plus any extra contacts passed in the `contacts` array (Lead's own `email`/`phone` columns are unchanged, still the primary/duplicate-checked contact)
- [x] `lead_activities` table + `POST /api/v1/leads/{id}/activities` — logs a Note/Meeting/Call/Comment against a lead (403/404 gated the same way as the rest of the Lead API); a Postgres trigger (`clock_timestamp()`, not `now()`, so it fires correctly even inside one transaction) bumps the parent lead's `updated_at` on every insert into `lead_activities`, including ones written directly with raw SQL
- [x] `GET /api/v1/leads/{id}` now returns the full single-lead detail: `owner_name`, every `lead_contacts` row, the full activity log (each with `created_by_name`/`created_at`) and an `activity_count`; `GET /api/v1/leads` (list) gained `owner_name` and `updated_at` per row
- [x] `GET /api/v1/users` opened up to Delivery SME as well (Sales Rep/Manager/Admin already could) — SME has full Lead access and needs the same Owner-dropdown data
- [x] Account CRUD + filter/search (owner, tier, company/owner-name search) — `POST/GET/PATCH/DELETE /api/v1/accounts`, same RBAC/ownership-scoping as Leads; gained `linkedin_url`; `POST /api/v1/leads/{id}/convert` turns a Lead into an Account (409 on double-conversion, 400 if no tier/owner-id was resolved — tier must now always come from the convert request, Lead no longer carries one), Lead gains `is_converted`
- [x] Contact CRUD — `POST/GET/PATCH/DELETE /api/v1/contacts`, `GET /api/v1/accounts/{id}/contacts` (now paginated); no `owner_id` of its own, access gated entirely through the parent Account's owner (Sales Rep/Manager/Admin, Delivery SME 403). Superseded by the `contact_accounts` refactor below — see that entry.
- [x] Deal CRUD + stage transitions + stage history — `POST/GET/PATCH/DELETE /api/v1/deals`, `GET /api/v1/deals/{id}/stage-history`, `GET /api/v1/accounts/{id}/deals`; Deal has its own `owner_id` independent of the account's owner, any stage can move to any other stage (no transition graph), every stage change (including creation) writes a `deal_stage_history` row
- [x] Deal "mark cold with reason" — `cold_reason` on Deal, required (400) whenever the resulting stage is `cold_deals` with no reason on record, whether set on create or via PATCH
- [x] Paginated list responses — `GET /api/v1/leads`, `/api/v1/accounts`, `/api/v1/deals`, and `/api/v1/accounts/{id}/contacts` now return `{items, total, limit, offset}` instead of a bare array, so the frontend can render "1-25 of N" without a second request
- [x] CORS middleware (`app/main.py`) — wildcard origins for now (safe: auth is a Bearer token, not a cookie, so no `allow_credentials` needed); without this, browsers/Flutter-web got a 404/405 on the `OPTIONS` preflight before ever reaching a route
- [x] Refresh tokens + logout — `POST /api/v1/auth/login` now also returns a `refresh_token` (opaque, DB-backed via new `refresh_tokens` table, 30-day expiry by default); `POST /api/v1/auth/refresh` exchanges it for a new access token (no rotation — same refresh token reused until logout/expiry); `POST /api/v1/auth/logout` (authenticated) revokes the caller's own refresh token, idempotently
- [x] Lead import from `.xlsx`/`.csv` — `GET /api/v1/leads/import/template`, `POST /api/v1/leads/import`
- [x] `POST /api/v1/accounts` ("Save & Convert to Account" on the New Lead form) now requires `domain` alongside `company`/`tier`/`owner_id`, and accepts a `contacts` list saved straight to the `contacts` table (`account_id` FK); only the first contact needs a name — later ones just add another email/phone and inherit it
- [x] Every Account response (`POST`/`GET`/`GET` list) now carries `owner_name`, `contact_count`, and `deal_count` to match the Accounts List screen's table columns; `GET /api/v1/accounts` gained an `industry` filter and its `search` now also matches `domain` (not just company/owner name)
- [x] `PATCH /api/v1/accounts/{id}` accepts the same `contacts` list as `POST` (appends, same first-contact-needs-a-name rule) — lets the Edit Account form add contacts in the same call
- [x] `GET /api/v1/accounts/{id}/overview` — the Account Overview screen in one call: account fields, `open_deal_value` (sum of non-closed/non-cold deal values), `key_contacts`, `active_deals`. `last_activity`/`next_step`/`total_arr` are always `null` — no backing model yet (no generic Account activity log, no ARR field); Pre-Sales Checklist deliberately left out of this endpoint for the same reason
- [x] Contact is now many-to-many with Account via a new `contact_accounts` join table (`contact_id`, `account_id`, `is_primary`, unique per pair, partial-unique index enforcing at most one primary contact per account — violating it raises `409`, never silently demotes the old primary). `POST /api/v1/accounts/{id}/contacts` is a single create-or-update route (same dispatch pattern as `POST /leads`): absent `contact_id` creates a Contact + its link (201), present `contact_id` updates that contact's fields and/or `is_primary` (200), creating the link if the contact wasn't already associated with that account (how an existing contact gets added to another account); `GET /api/v1/accounts/{id}/contacts` now includes `is_primary` per contact. Contact gained `linkedin_url`/`alternate_phone`. Standalone `POST/GET/PATCH/DELETE /api/v1/contacts` are now role-gated only, no per-account ownership check — there's no single owning account to check anymore.
- [x] Fixed `DELETE /api/v1/leads/{id}` 400 when a lead had activities/contacts — `Lead.contacts`/`Lead.activities` relationships now use `passive_deletes=True` so the ORM defers to the FK's existing `ON DELETE CASCADE` instead of trying to null the (`NOT NULL`) `lead_id` column itself
- [x] Profile self-service — `GET/PATCH /api/v1/users/me` (name/phone, email not editable here), `POST /api/v1/users/me/password` (current-password verified), `POST /api/v1/users/me/avatar` (image/png or image/jpeg, saved to local disk under `media/avatars/`, served via `/media` static mount); `User` gained `phone_number`/`avatar_url`/`last_login_at`, the last one stamped on every successful login
- [x] Swagger's Authorize button/padlocks now work — `app/core/deps.py` adds an `HTTPBearer` scheme so FastAPI's OpenAPI generator registers a security scheme, wired globally alongside `enforce_rbac` in `app/main.py`; real auth is still done entirely by `rbac_middleware`, this only makes the token show up in `/docs`
- [x] `POST /api/v1/users` reactivates a soft-deleted user instead of failing "Email already exists" — same email on a previously deleted row now overwrites name/role and reissues a generated password on that same user id (preserves history); a still-active duplicate email still 409s as before
- [x] Notifications: in-app bell/panel backed by `GET/PATCH/POST/DELETE /api/v1/notifications` (list with `unread_only`/`type` filters, `unread-count`, `PATCH /{id}` `{"is_read": bool}` to toggle one notification
read/unread, mark-all/bulk-read, bulk soft-delete) — populated on new-lead creation (same `LEADS_NOTIFY_ON_CREATE`-permission recipients as the existing admin email), lead reassignment, and deal stage changes, plus a `task_overdue` entry computed at read time from `Lead.next_follow_up_date` (no new Task entity, no scheduler); polling-based, no real-time push, no email mirroring
- [x] Global search — `GET /api/v1/search?q=` (auth-only, no specific permission gate) matches by name across Leads (first/last name), Accounts (company), Deals (deal name), and Contacts (first/last name); each result is `{id, label, name}` (`label` is the entity type), capped at 5 per type
- [x] Deal pipeline stages are now dynamic, admin-configurable rows (`deal_stages`, full CRUD via `POST/GET/PATCH/DELETE /api/v1/deal-stages`) instead of a fixed enum — `Deal.stage_id`/`DealStageHistory.from_stage_id`/`to_stage_id` replace the old `deal_stage` enum columns; `is_cold` on the stage (not a hardcoded stage-name check) now drives the cold-reason-required rule; a minimal `companies` table backs per-company stage grouping (single "Default" company seeded, no other table gets a `company_id`); `Deal` gained optional `tier` and a many-to-many `contact_ids` (via the `deal_contacts` join table, replacing the earlier single nullable `contact_id`) so a deal can have multiple stakeholder contacts; `GET /api/v1/deals` gained `view=list|board` (board groups the same filtered results by stage with per-column totals, unpaginated), `sort_by`/`sort_dir`, and account-company-name search; `GET /api/v1/deals/export` streams an xlsx of all role-scoped deals; `PATCH /api/v1/deals/generic-patch` is a small allowlisted single-field/record patch (`deals`/`deal_stage_history`/`deal_stages`)
- [x] Deal Activities — `deal_activities` table + `POST/GET/PATCH/DELETE /api/v1/deals/{id}/activities` (Note/Meeting/Call/Comment/Follow-up), mirroring Lead Activities exactly: `types`/`date_from`/`date_to` filters on list, only the deal owner may edit, delete gated by the new `deals.delete_any_activity` permission (Admin only by default, same pattern as `leads.delete_any_activity`)
- [x] Deal Documents — `deal_documents` table + `POST/GET/DELETE /api/v1/deals/{id}/documents` for uploading proposals/NDAs/contracts per deal (`application/pdf`, `.doc`, `.docx`, `image/png`, `image/jpeg`), saved to local disk under `media/deal_documents/` with a per-upload unique filename; upload/list/delete are gated the same way as the Deal record itself (ownership or `deals.view_all`, no extra owner-only rule); backed by a new shared class-based `FileUploadService` (`app/services/file_upload_service.py`) which `save_avatar` was refactored to use as well, so avatar and deal-document uploads share one code path
- [x] `GET /api/v1/leads/export` streams an xlsx of all role-scoped leads (Name, Email, Phone, Company, Source, Status, Owner), scoped/filterable the same way as `GET /api/v1/leads` (owner-or-unassigned unless `leads.view_all`; `source`/`status`/`search`, no pagination) — xlsx building for both Deal and Lead export now goes through a shared `rows_to_xlsx()` helper in the new `app/services/export_service.py` (openpyxl, no new dependency), which the Deal export route was refactored to use as well (same output, no behavior change)
- [x] Duplicate-email guard extended to standalone Contacts and a Lead's additional `lead_contacts` (previously only a Lead's own email was unique) — 409 on either, same `IntegrityError`-catch pattern as the existing Lead/User checks
- [x] Changing your own password (`POST /api/v1/users/me/password`) now forces re-login everywhere: `User.password_changed_at` is stamped, every refresh token is revoked, and the JWT gained an `iat` claim so even the current still-unexpired access token is rejected by `enforce_rbac` once it predates the change
- [x] `PasswordChange.new_password` now requires at least one uppercase, one lowercase, and one digit (min length 8 unchanged)
- [x] `linkedin_url` fields (Contact/Account/Lead/account-contact schemas) must be a full `http(s)://`+`linkedin.com` URL; `Contact.email`/`AccountContactUpsert.email` are now `EmailStr` like the rest of the app
- [x] Deal Activities gained an optional `title` field (`deal_activities.title`, `DealActivityCreate`/`Update`/`Read`) alongside the existing `note` body, for a short headline like "Discovery Call - Architecture Review"
- [x] Contact Detail + Contact List screens: `GET /api/v1/contacts` (filters: `owner_id`/`account_id`/`tier`/`is_primary`, `search`, paginated) and `GET /api/v1/contacts/{id}/overview` and `GET /api/v1/contacts/{id}/deals`. Contact itself carries no `owner_id`/`tier` (a Contact can link to more than one Account) — both derive owner/tier/account from the contact's *representative* Account link (its oldest `is_primary=True` `ContactAccount` row, or its oldest link overall if none is primary; all null if unlinked), while the list filters match if *any* of a contact's linked Accounts satisfies them. `tags`/`about`/`last_activity`/`task_count`/`log_count` are always `null` — no backing model yet, same pattern as `AccountOverviewRead`'s placeholders; `deal_count` is real (via `deal_contacts`)
- [x] Contact email is now globally unique AND required (`ix_contacts_email`, mirrors `Lead.email`) — `POST /api/v1/contacts` and `POST /api/v1/accounts/{id}/contacts` both `422` if `email` is missing and `409` if it's already used by any other contact, even under a different name or a different account (email, not name, is the identifying field). This removed the old "a contact needs at least one of email or phone" allowance on `AccountContactInput`/`AccountContactUpsert` — phone is now a supplementary field only, no longer an alternative to email
- [x] `POST /api/v1/accounts`'s `owner_id` is now optional -- an omitted/null owner defaults to the requesting user instead of failing validation
- [x] Bulk Contact import: `GET /api/v1/contacts/import/template` (xlsx/csv) and `POST /api/v1/contacts/import` — same pattern as Lead import (per-row errors collected instead of failing the whole batch, each successful row committed immediately). Imported contacts are always standalone, never linked to an Account (no company/account column in the sheet) — link them afterward via the existing Account Detail "Add Contact" flow
- [x] Unified Audit Log — `AuditLog` table + `app.services.audit_service.log_audit`, called from every mutating service (user create/deactivate/password-change, login/logout, lead/account/deal/contact create/update/delete, including contact create/update via `POST /accounts/{id}/contacts`); deal stage changes log the from/to stage names, lead-to-account conversion logs against both the lead and the new account; Admin-only `GET /api/v1/audit-log` (`audit_log.view` permission), paginated + filterable by `table_name`/`action`/`actor_id`/date range
- [x] Dashboard — collapsed from 7 separate routes into a single `GET /api/v1/dashboard` (accepts `period`, `granularity`, `limit`, `offset`, returns `summary`/`funnel`/`deal_distribution`/`leaderboard`/`drop_off_reasons`/`conversion_trend`/`activity_feed` together, one call for the whole page) over existing Lead/Deal/DealStage/DealStageHistory/activity data, no new tables; any authenticated user can view (no dedicated permission code). Target-vs-actual revenue, leaderboard %-of-target, and Upcoming Follow-ups deferred pending a Target/Quota and a Task model — see `docs/superpowers/specs/2026-07-28-dashboard-design.md`
- [x] Documents page — `GET /api/v1/documents` unions `AccountDocument`/`DealDocument` into one read-only list (`source`/`entity_id`/`entity_name` plus the usual file fields), sorted newest first, scoped the same way as the Account/Deal list endpoints (owner-only unless `accounts.view_all`/`deals.view_all`); auth-only route, no dedicated permission code (same pattern as global search). Upload/delete still only exist on the per-account/per-deal endpoints — this is read-only, pulled forward out of Milestone 3 for the sidebar Documents page
- [x] Unified export — `to_export=true` on the 8 existing lead/account/deal/contact GET routes (list + single-record) instead of separate export routes. `GET /api/v1/leads/export` and `GET /api/v1/deals/export` are removed (folded into `GET /leads`/`GET /deals` with the same filters). Bulk export is a single-sheet xlsx (same columns as before for leads/deals; new for accounts/contacts). Single-record export is a multi-sheet xlsx: detail fields on the first sheet, plus the same data as that resource's activities/contacts/deals/stage-history sub-routes on the following sheet(s)
- [x] Bug fix: `POST /api/v1/leads/{id}/convert` now also creates the primary Contact — previously it copied only company-level fields (`company`/`domain`/`tier`/`owner_id`/`linkedin_url`) onto the new Account and dropped the lead's person-level data (`first_name`/`last_name`/`email`/`phone`), leaving the converted Account with zero linked contacts. Reuses an existing `Contact` by email if one already exists (email is globally unique), otherwise creates one and links it via `ContactAccount(is_primary=True)`
- [x] Dashboard fixes: `period` drops `today` and gains `custom` (`start_date`/`end_date` query params, `422` if either is missing for a custom range) alongside the existing `this_week`/`this_month`; `summary` gains a `num_accounts` tile for the Leads to Accounts card. `deals_closed` (Closed Won only) is unchanged. Every section of the page is now consistently period-scoped: `funnel` counts deals that *entered* each stage (via `DealStageHistory`) during the period rather than a live snapshot, `deal_distribution` scopes to deals created in the period, `leaderboard` scopes revenue/win-count to deals closed in the period, `activity_feed` scopes to activity logged in the period, and `conversion_trend` now restricts its date range to the selected period (still bucketed by `granularity`) instead of always spanning all-time. `deals_in_pipeline` (open deals opened in the period) and `num_accounts` (accounts created in the period) also gained period-scoping with prior-period `change_pct`, replacing their earlier live-total behavior
- [x] `GET /api/v1/deals/{id}/stage-history` rows now include `changed_by_name`, so the Activity Timeline can show who moved the deal instead of just a raw user id. `DealStageHistory` gained an eager-joined `changed_by_user` relationship + `changed_by_name` property (same pattern as `DealActivity.created_by_name`); also fixed `list_stage_history`'s ordering, which only sorted by `created_at` and could return same-instant transitions (e.g. two quick PATCHes) in a non-deterministic order — now has `id` as a tiebreaker
- [x] New `dashboard.view` permission — `GET /api/v1/dashboard` is no longer open to any authenticated user; it now requires this permission, granted by default only to the Sales Manager starter role (Admin gets it automatically via the full-catalog grant). Sales Rep/Delivery SME get `403` unless an admin edits their role via `/roles` to add it
- [x] Bug fix: two `summary` tiles disagreed with a manual DB count. **Leads to Accounts** (`leads_to_accounts`) counted `Lead.status in (contacted, contact_in_future)` — a heuristic disconnected from actual qualification, since converting a lead never touches its `status`; it now counts leads that actually converted to an Account in the period (`Account.source_lead_id is not null`, by `Account.created_at`). **Deals in Pipeline** filtered by `Deal.created_at` in the period, so a deal opened last month but still open today silently dropped out of every later period's count; it's now a point-in-time snapshot (open as of the period's `end` date — today for `this_week`/`this_month`, the chosen `end_date` for a custom range), with `change_pct` comparing that snapshot to the same snapshot at the previous period's end. Also: `drop_off_reasons` displayed unscoped all-time `count`/`lost_value` while only `change_pct` was period-scoped (so a reason could show `count: 1` with `change_pct: -100%` for the same period); both now come from the same period-scoped query, with "Unknown" (no-history) rows falling back to `Deal.created_at` so they can still be dated


- [x] Forgot Password — `POST /api/v1/auth/forgot-password` (always 204, no user enumeration) emails a single-use, 1-hour reset token to the account if it exists and is active; `POST /api/v1/auth/reset-password` (token + new_password) updates the password and revokes every other active refresh token, forcing re-login elsewhere. New `password_reset_tokens` table, mirrors `refresh_tokens`' shape. Email currently carries the raw token (no frontend reset-page URL exists yet to link to)
- [x] Backend-changes-required cleanup: `GET /dashboard?period=custom` now `422`s on `end_date < start_date`. `DealRead` gains `account_name`/`owner_name`/`stage_name`/`stage_is_cold` (eager-joined `Deal.account`/`.stage`/`.owner`, same pattern as `DealStageHistory.changed_by_user`) so Kanban cards and the deals list/board no longer need three client-side N+1 lookups. `DealStageHistoryRead` gains `from_stage_name`/`to_stage_name`. Single-deal export's "Stage History" sheet and the Account/Contact exports' "Deals" sheet now write the stage name instead of the raw `stage_id` (deals-list export already did). `tier` is now repeatable (`?tier=gold&tier=silver`) on both `GET /deals` and its export. (`leaderboard[].owner_avatar_url` intentionally deferred — not requested this round)
- [x] Lead favourites — `Lead.is_favourite` bool (default `False`), settable via the existing `POST /api/v1/leads` upsert route (`{id, is_favourite}`, no new endpoint). `GET /api/v1/leads` now orders favourites to the top (`is_favourite DESC, created_at DESC`) so reps can pin the leads they care about instead of scrolling. Favourite/unfavourite toggles get a specific Audit Log description ("... (marked as favourite)" / "(removed from favourites)") instead of the generic "updated", and also auto-log a `LeadActivity` with `type: null` (`LeadActivity.type` is now nullable, no new type/table — `null` distinguishes system-generated rows from real note/call/meeting/comment/follow-up entries) so the toggle shows up in `GET /leads/{id}/activities` too. Both are true no-ops (no audit row, no activity row) when `is_favourite` is sent but doesn't actually change the lead's current value, in either direction
- [x] Bug fix: deleting an Account no longer raises a Postgres `IntegrityError` (`NotNullViolationError` on `contact_accounts.account_id`) when it has linked contacts — `Account.contact_accounts`'s `passive_deletes=True` still let SQLAlchemy null out the FK of an already-eager-loaded `ContactAccount` collection on parent delete; changed to `passive_deletes="all"` so it fully defers to the FK's own `ON DELETE CASCADE` regardless of what's loaded
- [x] Bug fix: `POST /api/v1/leads/{id}/convert` now carries over *all* of a Lead's contacts, not just the primary one — additional `LeadContact` rows (added via "+ Add another email" on the lead) were silently dropped on conversion. Each is reused-by-email or created and linked non-primary; since `LeadContact` has no name field, they inherit the lead's name (same convention as `_add_contacts`' nameless-extra-contact handling on direct Account creation)
- [x] `date_from`/`date_to` filter (on `created_at`) added to `GET /api/v1/leads`, `/accounts`, `/deals`, `/contacts`, `/users`, and the lead/account/deal/contact bulk-export path (`to_export=true`) — same param shape and `>=`/`<` semantics already used by the Lead/Account/Deal activity-log and Audit Log endpoints. Each of the 5 routes now also `422`s when `date_to` is before `date_from`, mirroring the existing `GET /dashboard?period=custom` end/start-date check — previously an inverted range silently returned zero rows instead of rejecting the request
- [x] New `lead_converted` notification — converting a Lead now notifies the resulting Account's owner (`recipient_id` = the resolved owner, which may differ from the lead's own owner if reassigned during conversion), same shape as the existing `LEAD_ASSIGNED` notify-on-owner-change. New `notification_type` Postgres enum value added via Alembic migration `535fddade94a`
- [x] Dashboard `funnel` redesigned: open stages (before Closed Won/Lost/Cold) are now a live count of deals currently in that stage, `period` doesn't affect them. Terminal stages are period-scoped by `Deal.updated_at`, same query as `deals_closed`/`leaderboard`, so e.g. the Closed Won bar always equals the Deals Closed tile for the same period. Previously counted `DealStageHistory` stage-entry events during the period, which double-counted a deal that bounced through (or re-entered) the same stage more than once in a window
- [x] Bug fix: `cold_reason` was only required when a deal's stage had `is_cold=true` — "Closed Lost" (`is_cold=false`) could be set with no reason at all, so `drop_off_reasons` (which requires `cold_reason` to be non-null) could only ever show Cold Deals data in practice, never actual lost-deal reasons. `create_deal`/`update_deal` now also require `cold_reason` when moving to "Closed Lost", same `400 ColdReasonRequiredError` as Cold stages
- [x] Bug fix — dashboard "Deals Closed" tile disagreed with `GET /deals` for the same date range (e.g. tile `2` vs one Closed Won row). Two causes, both fixed. **(1) `Deal.updated_at` was never a close date.** `Base.updated_at` carries `onupdate=now()`, so any edit — rename, value, owner, tier — bumped it: a deal won in March and renamed in August landed in August's `deals_closed`, its Closed Won funnel bar and the `leaderboard` revenue, while a deal won in August and edited in September silently left August, making past periods irreproducible. All three now date a close by the transition into the deal's current stage, read from `deal_stage_history` (`entered_current_stage_in`), which every stage move already writes — no migration, no `closed_at` column, existing data covered. `deals_in_pipeline` likewise reconstructs each deal's stage as of the period end (`stage_as_of`) instead of projecting today's stages backwards, so a deal that closed *after* the snapshot correctly counts as open then. **(2) The two endpoints answered different questions with no way to ask the same one.** `GET /deals` gains `date_field=created_at|closed_at` (default `created_at`, so existing callers are unaffected); `closed_at` filters by terminal-stage entry, making `?date_field=closed_at` alone return exactly the deals the tile counted — pass `stage_id` explicitly (e.g. `stage_id=<Closed Lost id>`) to reconcile the Closed Lost/Cold Deals funnel bars instead (see the follow-up bug fix below for why the implicit stage filter had to change). Applies to the list, board and `to_export=true` paths (one shared filter). Also: `date_to` on `GET /deals` is now **inclusive**, matching the dashboard's `end_date` (it was exclusive, silently dropping deals created on the requested end date); the stage-name constants and the terminal-stage predicate moved to `app/models/deal_stage.py` so `dashboard_service` and `deal_service` stop keeping separate copies; and `PATCH /deals/generic-patch` now writes a `deal_stage_history` row when it moves `stage_id`, which it previously skipped — harmless while closes were dated off `updated_at`, but it would have made such a move invisible to every close-based number
- [x] Dashboard tile drill-downs on `GET /api/v1/deals`: `stage_state=all|open|closed` (default `all`) makes the **Deals in Pipeline** tile reproducible — `?stage_state=open` with *no* date filter, since that tile is a point-in-time snapshot of open deals as of the period end and any date filter diverges from it (a deal opened in July and still open today is in the tile but outside an August range). `422` when combined with `date_field=closed_at`, which is contradictory. Kept server-side because `DealStage` exposes only `is_cold` — Closed Won/Closed Lost are indistinguishable from open stages in `GET /deal-stages`, so a client filtering by `stage_id` would have to hardcode those names and would silently miss any newly added stage. `stage_id` is now also repeatable (`?stage_id=3&stage_id=4`, same shape as `tier`; a single value is unchanged) — ids not names, because `DealStage` names are unique only per company (`UniqueConstraint(company_id, name)`) and `User` has no `company_id` to disambiguate against. Known limit, marked with a `ponytail:` comment: `stage_state=open` reads each deal's current stage, so it matches the tile for `this_week`/`this_month` but not for a past custom range, where the tile reconstructs stages as of the period end — `open_as_of=<date>` reusing `stage_as_of` would close that gap
- [x] Bug fix — `GET /deals?date_field=closed_at` (the dashboard's "Deals Closed" tile drill-down, no `stage_id`) returned every terminal-stage deal closed in the window (Closed Won *and* Closed Lost *and* cold), not just Closed Won, so it over-counted against a tile that's Closed-Won-only (`dashboard_service._count_deals_closed`) — e.g. tile `2` but the drill-down listed a 3rd, Closed Lost deal. `_deal_filters`'s implicit stage filter under `date_field=closed_at` is now Closed Won only when `stage_id` isn't given; an explicit `stage_id` (e.g. for the funnel's Closed Lost/Cold bars) still overrides it, unaffected. Frontend "Deals Closed" tile tap now calls this with no `stage_id`, matching the tile exactly
- [x] Bug fix — dashboard's Deal Distribution donut (by tier) disagreed with `GET /deals?date_field=closed_at` for an identical date range (e.g. donut `4 diamond, 2 gold` vs the list showing `5, 3`). `get_deal_distribution` was scoped by `Deal.created_at`, a different question than the list's `closed_at`-based filter: a deal created in-period but still open counted on the donut but not the list, and one created earlier but moved stages in-period counted on the list but not the donut. Re-scoped to `entered_current_stage_in` (`deal_stage_history`), the same predicate `deals_closed`/`funnel`/`leaderboard` already use, so it now agrees with the list by construction for the same range
- [x] Bug fix — `GET /deals?date_field=closed_at` (with an explicit `stage_id`, as the Deals list frontend always sends) dropped an open-stage deal from the results if it hadn't entered that stage within `date_from`/`date_to`, even though the dashboard's Pipeline Funnel counts that same open-stage bar as a live, period-agnostic snapshot (`dashboard_service.get_funnel`'s `in_scope` rule) — e.g. the funnel showed 1 deal in Qualified to Buy for a date range, but the same range on the Deals list showed 0. `_deal_filters` now only applies `entered_current_stage_in` to deals in a *terminal* stage; an open-stage deal in the requested `stage_id` set is returned unconditionally, matching the funnel by construction
- [ ] Static Pre-Sales Checklist per deal
- [ ] Activity log for Account (Lead's and Deal's are done; Account's is still open)
- [ ] Dashboard aggregation endpoints (funnel, target vs actual)

## Milestones (per Phase 1 kickoff)
- **Week 1** — Login, RBAC, Lead Management, Navigation
- **Week 2** — Accounts, Contacts, Deals, Activities, Notifications
- **Week 3** — Dashboard, QA/UAT, optimization, deployment
m