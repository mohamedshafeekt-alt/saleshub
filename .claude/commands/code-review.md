---
description: Review recent changes for scope drift, missing business rules, and convention adherence before calling a task done.
disable-model-invocation: true
---

Use the `code-reviewer` subagent to review the changes made in this
session (use `git diff` — read-only — to see what changed, since you
cannot commit or check out anything). Report blockers and suggestions
before this task is marked done in `README.md`.